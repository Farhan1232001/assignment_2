
/// Interface is essentially like an abstract class
/// but where allmethods are implicitly public and abstract
/// and where all fiellds are implicityl public, static, and final
interface Salableitem
{
    // 'public abstract' is implicitly added
    void sellCopy();
}